apiVersion: v1
kind: Service
metadata:
  name: php-service
spec:
  type: NodePort
  selector:
    app: php-devops
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
---

# This exposes your application.